<UserControl
    x:Class="Pilaster.Setup.Views.Pages.FinishPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    d:DesignHeight="400"
    d:DesignWidth="680"
    mc:Ignorable="d">

    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
        <ui:SymbolIcon
            x:Name="ResultIcon"
            Margin="0,0,0,16"
            HorizontalAlignment="Center"
            FontSize="56"
            Foreground="{DynamicResource SystemFillColorSuccessBrush}"
            RenderTransformOrigin="0.5,0.5"
            Symbol="CheckmarkCircle24">
            <ui:SymbolIcon.RenderTransform>
                <ScaleTransform x:Name="ResultIconScale" ScaleX="0.5" ScaleY="0.5" />
            </ui:SymbolIcon.RenderTransform>
        </ui:SymbolIcon>

        <TextBlock
            x:Name="TitleText"
            Margin="0,0,0,8"
            HorizontalAlignment="Center"
            FontSize="20"
            FontWeight="SemiBold" />

        <TextBlock
            x:Name="DetailText"
            MaxWidth="420"
            HorizontalAlignment="Center"
            Foreground="{DynamicResource TextFillColorSecondaryBrush}"
            TextAlignment="Center"
            TextWrapping="Wrap" />

        <CheckBox
            x:Name="LaunchCheckBox"
            Margin="0,20,0,0"
            HorizontalAlignment="Center"
            Content="A Pilaster indítása most" />

        <ui:HyperlinkButton
            x:Name="ChangelogLink"
            Margin="0,8,0,0"
            HorizontalAlignment="Center"
            Click="OnChangelogClick"
            Content="Változásnapló megtekintése" />
    </StackPanel>
</UserControl>
