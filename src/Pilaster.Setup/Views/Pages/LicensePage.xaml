<UserControl
    x:Class="Pilaster.Setup.Views.Pages.LicensePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="400"
    d:DesignWidth="680"
    mc:Ignorable="d">

    <Grid Margin="32,24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock
            Grid.Row="0"
            Margin="0,0,0,16"
            FontSize="20"
            FontWeight="SemiBold"
            Text="Licencszerződés" />

        <Border
            Grid.Row="1"
            Background="{DynamicResource CardBackgroundFillColorDefaultBrush}"
            BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}"
            BorderThickness="1"
            CornerRadius="8">
            <ScrollViewer Margin="16" VerticalScrollBarVisibility="Auto">
                <TextBlock
                    x:Name="LicenseText"
                    FontFamily="Consolas"
                    FontSize="12"
                    TextWrapping="Wrap" />
            </ScrollViewer>
        </Border>

        <CheckBox
            x:Name="AcceptCheckBox"
            Grid.Row="2"
            Margin="0,16,0,0"
            Checked="OnAcceptChanged"
            Content="Elfogadom a licencfeltételeket"
            Unchecked="OnAcceptChanged" />
    </Grid>
</UserControl>
