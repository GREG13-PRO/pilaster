<ui:FluentWindow
    x:Class="Pilaster.App.Views.SettingsWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:Pilaster.App.Controls"
    xmlns:loc="clr-namespace:Pilaster.App.Localization"
    xmlns:media="clr-namespace:System.Windows.Media;assembly=PresentationCore"
    xmlns:metadata="clr-namespace:Pilaster.Core.Metadata;assembly=Pilaster.Core"
    xmlns:settings="clr-namespace:Pilaster.Core.Settings;assembly=Pilaster.Core"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:vm="clr-namespace:Pilaster.App.ViewModels"
    Title="{loc:Loc Cmd_Settings}"
    Width="900"
    Height="780"
    MinWidth="760"
    MinHeight="520"
    Icon="pack://application:,,,/Pilaster;component/Assets/app.ico"
    ExtendsContentIntoTitleBar="True"
    WindowBackdropType="Mica"
    WindowCornerPreference="Round"
    WindowStartupLocation="CenterOwner">

    <ui:FluentWindow.Resources>

        <!--  Egy beállítás-kártya: cím, leírás, jobb oldalt a vezérlő  -->
        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="{DynamicResource CardBackgroundFillColorDefaultBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource CardStrokeColorDefaultBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="CornerRadius" Value="8" />
            <Setter Property="Padding" Value="16,13" />
            <Setter Property="Margin" Value="0,0,0,8" />
        </Style>

        <Style x:Key="SectionHeaderStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="15" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Margin" Value="2,18,0,8" />
        </Style>

        <Style x:Key="CaptionStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="12" />
            <Setter Property="Foreground" Value="{DynamicResource TextFillColorSecondaryBrush}" />
            <Setter Property="TextWrapping" Value="Wrap" />
            <Setter Property="Margin" Value="0,3,0,0" />
        </Style>

    </ui:FluentWindow.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <ui:TitleBar
            Grid.Row="0"
            Title="{loc:Loc Cmd_Settings}"
            ShowMaximize="False" />

        <Grid Grid.Row="1">
        <Border Background="{DynamicResource GlassPanelBrush}">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="212" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!--  Keresőmező: névre, leírásra és rejtett kulcsszavakra egyaránt keres (spec F6)  -->
            <ui:TextBox
                Grid.Row="0"
                Grid.Column="0"
                Grid.ColumnSpan="2"
                Margin="18,4,18,10"
                PlaceholderText="{loc:Loc Settings_SearchPlaceholder}"
                Text="{Binding SearchText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
                <ui:TextBox.Icon>
                    <ui:SymbolIcon Symbol="Search24" />
                </ui:TextBox.Icon>
            </ui:TextBox>

            <!--  Bal oldali kategórialista  -->
            <ListBox
                Grid.Row="1"
                Grid.Column="0"
                Margin="10,0,4,0"
                Background="Transparent"
                BorderThickness="0"
                ItemsSource="{Binding Categories}"
                SelectedItem="{Binding SelectedCategory, Mode=TwoWay}">
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="Padding" Value="{DynamicResource TokenRowPadding}" />
                        <Setter Property="Margin" Value="{DynamicResource TokenRowMargin}" />
                        <Setter Property="MinHeight" Value="{DynamicResource TokenRowHeight}" />
                        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                        <Setter Property="Visibility" Value="{Binding IsVisible, Converter={StaticResource BoolToVisibility}}" />
                    </Style>
                </ListBox.ItemContainerStyle>
                <ListBox.ItemTemplate>
                    <DataTemplate DataType="{x:Type vm:SettingsCategoryViewModel}">
                        <StackPanel Orientation="Horizontal">
                            <ui:SymbolIcon
                                Margin="0,0,10,0"
                                VerticalAlignment="Center"
                                FontSize="16"
                                Symbol="{Binding Icon}" />
                            <TextBlock
                                VerticalAlignment="Center"
                                Text="{Binding Title}"
                                TextTrimming="CharacterEllipsis" />
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

            <!--  Keresési találatok — a kategória tartalma helyett  -->
            <Border
                Grid.Row="1"
                Grid.Column="1"
                Margin="10,0,18,0"
                Visibility="{Binding IsSearching, Converter={StaticResource BoolToVisibility}}">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <ItemsControl ItemsSource="{Binding SearchResults}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate DataType="{x:Type vm:SettingsDescriptor}">
                                <ui:Button
                                    Margin="0,0,0,6"
                                    Padding="14,10"
                                    HorizontalAlignment="Stretch"
                                    HorizontalContentAlignment="Left"
                                    Appearance="Secondary"
                                    Command="{Binding DataContext.GoToSettingCommand, RelativeSource={RelativeSource AncestorType=ui:FluentWindow}}"
                                    CommandParameter="{Binding}"
                                    CornerRadius="8">
                                    <StackPanel>
                                        <TextBlock FontWeight="SemiBold" Text="{Binding TitleKey, Converter={StaticResource LocalizeKey}}" />
                                        <TextBlock
                                            Margin="0,3,0,0"
                                            FontSize="12"
                                            Foreground="{DynamicResource TokenTextSecondary}"
                                            Text="{Binding DescriptionKey, Converter={StaticResource LocalizeKey}}"
                                            TextWrapping="Wrap" />
                                    </StackPanel>
                                </ui:Button>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </Border>

            <ScrollViewer
                x:Name="ContentScroll"
                Grid.Row="1"
                Grid.Column="1"
                Padding="10,0,18,4"
                VerticalScrollBarVisibility="Auto"
                Visibility="{Binding IsSearching, Converter={StaticResource BoolToVisibility}, ConverterParameter=invert}">
            <StackPanel>

                <!--  ===== Megjelenés =====  -->
                <StackPanel Visibility="{Binding SelectedCategory.Id, Converter={StaticResource CategoryVisible}, ConverterParameter=appearance}">
                <TextBlock Style="{StaticResource SectionHeaderStyle}" Text="{loc:Loc Settings_Appearance}" />

                <Border Style="{StaticResource CardStyle}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" Margin="0,0,16,0">
                            <TextBlock Text="{loc:Loc Settings_Theme}" />
                            <TextBlock Style="{StaticResource CaptionStyle}" Text="{loc:Loc Settings_ThemeHint}" />
                        </StackPanel>
                        <ComboBox
                            Grid.Column="1"
                            Width="200"
                            VerticalAlignment="Center"
                            ItemsSource="{Binding Themes}"
                            SelectedItem="{Binding SelectedTheme, Mode=TwoWay}">
                            <ComboBox.ItemTemplate>
                                <DataTemplate DataType="{x:Type settings:ThemeMode}">
                                    <TextBlock Text="{Binding Converter={StaticResource ThemeMode}}" />
                                </DataTemplate>
                            </ComboBox.ItemTemplate>
                        </ComboBox>
                    </Grid>
                </Border>

                <Border Style="{StaticResource CardStyle}">
                    <StackPanel>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0" Margin="0,0,16,0">
                                <TextBlock Text="{loc:Loc Settings_Accent}" />
                                <TextBlock Style="{StaticResource CaptionStyle}" Text="{loc:Loc Settings_AccentHint}" />
                            </StackPanel>
                            <StackPanel
                                Grid.Column="1"
                                VerticalAlignment="Center"
                                Orientation="Horizontal">
                                <TextBlock
                                    Margin="0,0,8,0"
                                    VerticalAlignment="Center"
                                    Text="{loc:Loc Settings_AccentUseSystem}" />
                                <ui:ToggleSwitch IsChecked="{Binding UseSystemAccent, Mode=TwoWay}" />
                            </StackPanel>
                        </Grid>

                        <!--
                            A paletta és a hex mező letiltva, amíg a rendszerszínt
                            követjük — enélkül úgy tűnne, mintha egyedi szín is
                            hatna, holott a kapcsoló felülírná a következő
                            rendszertéma-váltáskor.
                        -->
                        <ItemsControl
                            Margin="0,14,0,0"
                            IsEnabled="{Binding UseSystemAccent, Converter={StaticResource InvertBool}}"
                            ItemsSource="{Binding AccentPresets}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <WrapPanel />
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate DataType="{x:Type media:Color}">
                                    <ui:Button
                                        Width="32"
                                        Height="32"
                                        Margin="0,0,8,8"
                                        Padding="0"
                                        Appearance="Transparent"
                                        Command="{Binding DataContext.SelectAccentPresetCommand, RelativeSource={RelativeSource AncestorType=ui:FluentWindow}}"
                                        CommandParameter="{Binding}"
                                        CornerRadius="16">
                                        <Ellipse
                                            Width="24"
                                            Height="24"
                                            Fill="{Binding Converter={StaticResource ColorToBrush}}" />
                                    </ui:Button>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>

                        <Grid Margin="0,10,0,0" IsEnabled="{Binding UseSystemAccent, Converter={StaticResource InvertBool}}">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <Ellipse
                                Grid.Column="0"
                                Width="24"
                                Height="24"
                                Margin="0,0,10,0"
                                Fill="{Binding CurrentAccentColor, Converter={StaticResource ColorToBrush}}" />
                            <ui:TextBox
                                Grid.Column="1"
                                VerticalAlignment="Center"
                                Text="{Binding AccentHexInput, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                            <ui:Button
                                Grid.Column="2"
                                Margin="8,0,0,0"
                                Command="{Binding ApplyAccentHexCommand}"
                                Content="{loc:Loc Cmd_Apply}" />
                        </Grid>
                        <TextBlock
                            Margin="0,6,0,0"
                            Foreground="{DynamicResource TokenDanger}"
                            Text="{Binding AccentHexError}"
                            Visibility="{Binding AccentHexError, Converter={StaticResource NullToCollapsed}}" />
                    </StackPanel>
                </Border>

                <Border Style="{StaticResource CardStyle}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" Margin="0,0,16,0">
                            <TextBlock Text="{loc:Loc Settings_Animations}" />
                            <TextBlock Style="{StaticResource CaptionStyle}" Text="{loc:Loc Settings_AnimationsHint}" />
                        </StackPanel>
                        <ComboBox
                            Grid.Column="1"
                            Width="200"
                            VerticalAlignment="Center"
                            ItemsSource="{Binding AnimationLevels}"
                            SelectedItem="{Binding SelectedAnimationLevel, Mode=TwoWay}">
                            <ComboBox.ItemTemplate>
                                <DataTemplate DataType="{x:Type settings:AnimationLevel}">
                                    <TextBlock Text="{Binding Converter={StaticResource AnimationLevel}}" />
                                </DataTemplate>
                            </ComboBox.ItemTemplate>
                        </ComboBox>
                    </Grid>
                </Border>

                <Border Style="{StaticResource CardStyle}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" Margin="0,0,16,0">
                            <TextBlock Text="{loc:Loc Settings_LiquidGlass}" />
                            <TextBlock Style="{StaticResource CaptionStyle}" Text="{loc:Loc Settings_LiquidGlassHint}" />
                        </StackPanel>
                        <ui:ToggleSwitch
                            Grid.Column="1"
                            VerticalAlignment="Center"
                            IsChecked="{Binding LiquidGlassEnabled, Mode=TwoWay}" />
                    </Grid>
                </Border>

                </StackPanel>

                <StackPanel Visibility="{Binding SelectedCategory.Id, Converter={StaticResource CategoryVisible}, ConverterParameter=integrations}">
                <!--  ===== Panelek =====  -->
                <StackPanel Visibility="{Binding SelectedCategory.Id, Converter={StaticResource CategoryVisible}, ConverterParameter=panes}">
                    <TextBlock Style="{StaticResource SectionHeaderStyle}" Text="{loc:Loc SettingsCat_Panes}" />

                    <Border Style="{StaticResource CardStyle}" Tag="panes.vertical">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0" Margin="0,0,16,0">
                                <TextBlock Text="{loc:Loc Settings_DualPaneVertical}" />
                                <TextBlock Style="{StaticResource CaptionStyle}" Text="{loc:Loc Settings_DualPaneVerticalHint}" />
                            </StackPanel>
                            <ui:ToggleSwitch
                                Grid.Column="1"
                                VerticalAlignment="Center"
                                IsChecked="{Binding DualPaneVertical, Mode=TwoWay}" />
                        </Grid>
                    </Border>

                    <Border Style="{StaticResource CardStyle}" Tag="panes.splitRatio">
                        <StackPanel>
                            <TextBlock Text="{loc:Loc Settings_SplitRatio}" />
                            <TextBlock Style="{StaticResource CaptionStyle}" Text="{loc:Loc Settings_SplitRatioHint}" />
                        </StackPanel>
                    </Border>

                    <Border Style="{StaticResource CardStyle}" Tag="panes.rowStriping">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0" Margin="0,0,16,0">
                                <TextBlock Text="{loc:Loc Settings_RowStriping}" />
                                <TextBlock Style="{StaticResource CaptionStyle}" Text="{loc:Loc Settings_RowStripingHint}" />
                            </StackPanel>
                            <ui:ToggleSwitch
                                Grid.Column="1"
                                VerticalAlignment="Center"
                                IsChecked="{Binding DualPaneRowStriping, Mode=TwoWay}" />
                        </Grid>
                    </Border>

                    <Border Style="{StaticResource CardStyle}" Tag="panes.columnWidths">
                        <StackPanel>
                            <TextBlock Text="{loc:Loc Settings_ColumnWidths}" />
                            <TextBlock Style="{StaticResource CaptionStyle}" Text="{loc:Loc Settings_ColumnWidthsHint}" />
                        </StackPanel>
                    </Border>
                </StackPanel>

                <!--  ===== Fájllista =====  -->
                <StackPanel Visibility="{Binding SelectedCategory.Id, Converter={StaticResource CategoryVisible}, ConverterParameter=filelist}">
                    <TextBlock Style="{StaticResource SectionHeaderStyle}" Text="{loc:Loc SettingsCat_FileList}" />

                    <Border Style="{StaticResource CardStyle}" Tag="filelist.system">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0" Margin="0,0,16,0">
                                <TextBlock Text="{loc:Loc Settings_ShowSystem}" />
                                <TextBlock Style="{StaticResource CaptionStyle}" Text="{loc:Loc Settings_ShowSystemHint}" />
                            </StackPanel>
                            <ui:ToggleSwitch Grid.Column="1" VerticalAlignment="Center" IsChecked="{Binding ShowSystemItems, Mode=TwoWay}" />
                        </Grid>
                    </Border>

                    <Border Style="{StaticResource CardStyle}" Tag="filelist.extensions">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0" Margin="0,0,16,0">
                                <TextBlock Text="{loc:Loc Settings_ShowExtensions}" />
                                <TextBlock Style="{StaticResource CaptionStyle}" Text="{loc:Loc Settings_ShowExtensionsHint}" />
                            </StackPanel>
                            <ui:ToggleSwitch Grid.Column="1" VerticalAlignment="Center" IsChecked="{Binding ShowExtensions, Mode=TwoWay}" />
                        </Grid>
                    </Border>

                    <Border Style="{StaticResource CardStyle}" Tag="filelist.foldersFirst">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0" Margin="0,0,16,0">
                                <TextBlock Text="{loc:Loc Settings_FoldersFirst}" />
                                <TextBlock Style="{StaticResource CaptionStyle}" Text="{loc:Loc Settings_FoldersFirstHint}" />
                            </StackPanel>
                            <ui:ToggleSwitch Grid.Column="1" VerticalAlignment="Center" IsChecked="{Binding FoldersFirst, Mode=TwoWay}" />
                        </Grid>
                    </Border>

                    <Border Style="{StaticResource CardStyle}" Tag="filelist.sizeUnits">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0" Margin="0,0,16,0">
                                <TextBlock Text="{loc:Loc Settings_BinarySizeUnits}" />
                                <TextBlock Style="{StaticResource CaptionStyle}" Text="{loc:Loc Settings_BinarySizeUnitsHint}" />
                            </StackPanel>
                            <ui:ToggleSwitch Grid.Column="1" VerticalAlignment="Center" IsChecked="{Binding BinarySizeUnits, Mode=TwoWay}" />
                        </Grid>
                    </Border>
                </StackPanel>

                <!--  ===== Jobbklikk menü =====  -->
                <StackPanel Visibility="{Binding SelectedCategory.Id, Converter={StaticResource CategoryVisible}, ConverterParameter=contextmenu}">
                    <TextBlock Style="{StaticResource SectionHeaderStyle}" Text="{loc:Loc SettingsCat_ContextMenu}" />

                    <!--  Megjelenítési mód (spec v1.0.3) — a legfontosabb kapcsoló, ezért legfelül.  -->
                    <Border Style="{StaticResource CardStyle}" Tag="contextmenu.mode">
                        <StackPanel>
                            <TextBlock Margin="0,0,0,10" Text="{loc:Loc Settings_ContextMenuMode}" />

                            <RadioButton
                                Margin="0,0,0,10"
                                GroupName="ContextMenuMode"
                                IsChecked="{Binding ContextMenuMode, Mode=TwoWay, Converter={StaticResource EnumToBool}, ConverterParameter=Windows}">
                                <StackPanel Margin="8,0,0,0">
                                    <TextBlock Text="{loc:Loc Settings_ContextMenuMode_Windows}" />
                                    <TextBlock Style="{StaticResource CaptionStyle}" Text="{loc:Loc Settings_ContextMenuMode_WindowsHint}" />
                                </StackPanel>
                            </RadioButton>

                            <RadioButton
                                GroupName="ContextMenuMode"
                                IsChecked="{Binding ContextMenuMode, Mode=TwoWay, Converter={StaticResource EnumToBool}, ConverterParameter=Pilaster}">
                                <StackPanel Margin="8,0,0,0">
                                    <TextBlock Text="{loc:Loc Settings_ContextMenuMode_Pilaster}" />
                                    <TextBlock Style="{StaticResource CaptionStyle}" Text="{loc:Loc Settings_ContextMenuMode_PilasterHint}" />
                                </StackPanel>
                            </RadioButton>
                        </StackPanel>
                    </Border>

                    <Border Style="{StaticResource CardStyle}" Tag="contextmenu.shell">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0" Margin="0,0,16,0">
                                <TextBlock Text="{loc:Loc Settings_ShellExtensions}" />
                                <TextBlock Style="{StaticResource CaptionStyle}" Text="{loc:Loc Settings_ShellExtensionsHint}" />
                            </StackPanel>
                            <ui:ToggleSwitch Grid.Column="1" VerticalAlignment="Center" IsChecked="{Binding ShellExtensionsEnabled, Mode=TwoWay}" />
                        </Grid>
                    </Border>

                    <Border Style="{StaticResource CardStyle}" Tag="contextmenu.timeout" IsEnabled="{Binding ShellExtensionsEnabled}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0" Margin="0,0,16,0">
                                <TextBlock Text="{loc:Loc Settings_ShellTimeout}" />
                                <TextBlock Style="{StaticResource CaptionStyle}" Text="{loc:Loc Settings_ShellTimeoutHint}" />
                            </StackPanel>
                            <ui:NumberBox
                                Grid.Column="1"
                                Width="120"
                                VerticalAlignment="Center"
                                Maximum="5000"
                                Minimum="50"
                                Value="{Binding ShellMenuTimeoutMs, Mode=TwoWay}" />
                        </Grid>
                    </Border>

                    <Border Style="{StaticResource CardStyle}" Tag="contextmenu.section" IsEnabled="{Binding ShellExtensionsEnabled}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0" Margin="0,0,16,0">
                                <TextBlock Text="{loc:Loc Settings_ShellOwnSection}" />
                                <TextBlock Style="{StaticResource CaptionStyle}" Text="{loc:Loc Settings_ShellOwnSectionHint}" />
                            </StackPanel>
                            <ui:ToggleSwitch Grid.Column="1" VerticalAlignment="Center" IsChecked="{Binding ShellItemsInOwnSection, Mode=TwoWay}" />
                        </Grid>
                    </Border>

                    <!--  Előretöltés kijelöléskor (spec A2, v1.0.2)  -->
                    <Border Style="{StaticResource CardStyle}" Tag="contextmenu.preload" IsEnabled="{Binding ShellExtensionsEnabled}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0" Margin="0,0,16,0">
                                <TextBlock Text="{loc:Loc Settings_ContextMenuPreload}" />
                                <TextBlock Style="{StaticResource CaptionStyle}" Text="{loc:Loc Settings_ContextMenuPreloadHint}" />
                            </StackPanel>
                            <ui:ToggleSwitch Grid.Column="1" VerticalAlignment="Center" IsChecked="{Binding ContextMenuPreloadEnabled, Mode=TwoWay}" />
                        </Grid>
                    </Border>

                    <!--  Összeomlás utáni visszakapcsolás (spec P3)  -->
                    <Border Style="{StaticResource CardStyle}" Visibility="{Binding ShellDisabledAfterCrash, Converter={StaticResource BoolToVisibility}}">
                        <StackPanel>
                            <TextBlock Text="{loc:Loc Settings_ShellReenable}" />
                            <TextBlock Style="{StaticResource CaptionStyle}" Text="{loc:Loc Settings_ShellReenableHint}" />
                            <StackPanel Margin="0,6,0,0" Orientation="Horizontal" Visibility="{Binding ShellCrashPath, Converter={StaticResource NullToCollapsed}}">
                                <TextBlock
                                    Margin="0,0,6,0"
                                    FontSize="11"
                                    Foreground="{DynamicResource TokenTextMuted}"
                                    Text="{loc:Loc Settings_ShellCrashPath}" />
                                <TextBlock
                                    FontSize="11"
                                    Foreground="{DynamicResource TokenWarning}"
                                    Text="{Binding ShellCrashPath}"
                                    TextTrimming="CharacterEllipsis" />
                            </StackPanel>
                            <ui:Button
                                Margin="0,10,0,0"
                                HorizontalAlignment="Left"
                                Command="{Binding ReenableShellExtensionsCommand}"
                                Content="{loc:Loc Settings_ShellReenable}" />
                        </StackPanel>
                    </Border>
                    <Border Style="{StaticResource CardStyle}" Tag="contextmenu.blacklist" IsEnabled="{Binding ShellExtensionsEnabled}">
                        <StackPanel>
                            <TextBlock Text="{loc:Loc Settings_ShellBlacklist}" />
                            <TextBlock Style="{StaticResource CaptionStyle}" Text="{loc:Loc Settings_ShellBlacklistHint}" />
                            <ui:TextBox
                                MinHeight="80"
                                Margin="0,10,0,0"
                                AcceptsReturn="True"
                                Text="{Binding ShellBlacklistText, Mode=TwoWay, UpdateSourceTrigger=LostFocus}"
                                TextWrapping="NoWrap"
                                VerticalScrollBarVisibility="Auto" />
                        </StackPanel>
                    </Border>
                </StackPanel>

                <!--  ===== Szerkesztő =====  -->
                <StackPanel Visibility="{Binding SelectedCategory.Id, Converter={StaticResource CategoryVisible}, ConverterParameter=editor}">
                    <TextBlock Style="{StaticResource SectionHeaderStyle}" Text="{loc:Loc SettingsCat_Editor}" />

                    <Border Style="{StaticResource CardStyle}" Tag="editor.font">
                        <StackPanel>
                            <TextBlock Text="{loc:Loc Settings_EditorFont}" />
                            <TextBlock Style="{StaticResource CaptionStyle}" Text="{loc:Loc Settings_EditorFontHint}" />
                            <Grid Margin="0,10,0,0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <ui:TextBox Grid.Column="0" Margin="0,0,8,0" Text="{Binding EditorFontFamily, Mode=TwoWay, UpdateSourceTrigger=LostFocus}" />
                                <ui:NumberBox
                                    Grid.Column="1"
                                    Width="100"
                                    Maximum="48"
                                    Minimum="6"
                                    Value="{Binding EditorFontSize, Mode=TwoWay}" />
                            </Grid>
                        </StackPanel>
                    </Border>

                    <Border Style="{StaticResource CardStyle}" Tag="editor.tabWidth">
                        <StackPanel>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0" Margin="0,0,16,0">
                                    <TextBlock Text="{loc:Loc Settings_EditorTabWidth}" />
                                    <TextBlock Style="{StaticResource CaptionStyle}" Text="{loc:Loc Settings_EditorTabWidthHint}" />
                                </StackPanel>
                                <ui:NumberBox
                                    Grid.Column="1"
                                    Width="100"
                                    VerticalAlignment="Center"
                                    Maximum="16"
                                    Minimum="1"
                                    Value="{Binding EditorTabWidth, Mode=TwoWay}" />
                            </Grid>
                            <CheckBox
                                Margin="0,10,0,0"
                                Content="{loc:Loc Settings_EditorInsertSpaces}"
                                IsChecked="{Binding EditorInsertSpaces, Mode=TwoWay}" />
                        </StackPanel>
                    </Border>

                    <Border Style="{StaticResource CardStyle}" Tag="editor.wordWrap">
                        <StackPanel>
                            <CheckBox Content="{loc:Loc Settings_EditorWordWrap}" IsChecked="{Binding EditorWordWrap, Mode=TwoWay}" />
                            <CheckBox Margin="0,8,0,0" Content="{loc:Loc Settings_EditorLineNumbers}" IsChecked="{Binding EditorShowLineNumbers, Mode=TwoWay}" />
                            <TextBlock Style="{StaticResource CaptionStyle}" Text="{loc:Loc Settings_EditorWordWrapHint}" />
                        </StackPanel>
                    </Border>

                    <Border Style="{StaticResource CardStyle}" Tag="editor.encoding">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0" Margin="0,0,16,0">
                                <TextBlock Text="{loc:Loc Settings_EditorEncoding}" />
                                <TextBlock Style="{StaticResource CaptionStyle}" Text="{loc:Loc Settings_EditorEncodingHint}" />
                            </StackPanel>
                            <ComboBox
                                Grid.Column="1"
                                Width="160"
                                VerticalAlignment="Center"
                                ItemsSource="{Binding Encodings}"
                                SelectedItem="{Binding EditorDefaultEncoding, Mode=TwoWay}" />
                        </Grid>
                    </Border>

                    <Border Style="{StaticResource CardStyle}" Tag="editor.lineEnding">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0" Margin="0,0,16,0">
                                <TextBlock Text="{loc:Loc Settings_EditorLineEnding}" />
                                <TextBlock Style="{StaticResource CaptionStyle}" Text="{loc:Loc Settings_EditorLineEndingHint}" />
                            </StackPanel>
                            <ComboBox
                                Grid.Column="1"
                                Width="160"
                                VerticalAlignment="Center"
                                ItemsSource="{Binding LineEndings}"
                                SelectedItem="{Binding EditorDefaultLineEnding, Mode=TwoWay}" />
                        </Grid>
                    </Border>

                    <Border Style="{StaticResource CardStyle}" Tag="editor.window">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0" Margin="0,0,16,0">
                                <TextBlock Text="{loc:Loc Settings_EditorWindow}" />
                                <TextBlock Style="{StaticResource CaptionStyle}" Text="{loc:Loc Settings_EditorWindowHint}" />
                            </StackPanel>
                            <ui:ToggleSwitch Grid.Column="1" VerticalAlignment="Center" IsChecked="{Binding EditorInSeparateWindow, Mode=TwoWay}" />
                        </Grid>
                    </Border>
                </StackPanel>

                <!--  ===== Speciális: naplózás és mappák =====  -->
                <StackPanel Visibility="{Binding SelectedCategory.Id, Converter={StaticResource CategoryVisible}, ConverterParameter=advanced}">
                    <TextBlock Style="{StaticResource SectionHeaderStyle}" Text="{loc:Loc SettingsCat_Advanced}" />

                    <Border Style="{StaticResource CardStyle}" Tag="advanced.logLevel">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0" Margin="0,0,16,0">
                                <TextBlock Text="{loc:Loc Settings_LogLevel}" />
                                <TextBlock Style="{StaticResource CaptionStyle}" Text="{loc:Loc Settings_LogLevelHint}" />
                                <Border
                                    Margin="0,6,0,0"
                                    Padding="6,2"
                                    HorizontalAlignment="Left"
                                    Background="{DynamicResource TokenBgApp}"
                                    BorderBrush="{DynamicResource TokenBorder}"
                                    BorderThickness="1"
                                    CornerRadius="4">
                                    <TextBlock
                                        FontSize="11"
                                        Foreground="{DynamicResource TokenWarning}"
                                        Text="{loc:Loc Settings_RestartRequired}" />
                                </Border>
                            </StackPanel>
                            <ComboBox
                                Grid.Column="1"
                                Width="160"
                                VerticalAlignment="Center"
                                ItemsSource="{Binding LogLevels}"
                                SelectedItem="{Binding LogLevel, Mode=TwoWay}" />
                        </Grid>
                    </Border>

                    <Border Style="{StaticResource CardStyle}" Tag="advanced.logFile">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0" Margin="0,0,16,0">
                                <TextBlock Text="{loc:Loc Settings_OpenLog}" />
                                <TextBlock Style="{StaticResource CaptionStyle}" Text="{loc:Loc Settings_OpenLogHint}" />
                            </StackPanel>
                            <ui:Button
                                Grid.Column="1"
                                VerticalAlignment="Center"
                                Click="OnOpenLogClick"
                                Content="{loc:Loc Cmd_Open}" />
                        </Grid>
                    </Border>

                    <Border Style="{StaticResource CardStyle}" Tag="advanced.configFolder">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0" Margin="0,0,16,0">
                                <TextBlock Text="{loc:Loc Settings_OpenConfig}" />
                                <TextBlock Style="{StaticResource CaptionStyle}" Text="{loc:Loc Settings_OpenConfigHint}" />
                            </StackPanel>
                            <ui:Button
                                Grid.Column="1"
                                VerticalAlignment="Center"
                                Click="OnOpenConfigFolderClick"
                                Content="{loc:Loc Cmd_Open}" />
                        </Grid>
                    </Border>
                </StackPanel>

                <!--  ===== Általános: indulás és munkamenet =====  -->
                <StackPanel Visibility="{Binding SelectedCategory.Id, Converter={StaticResource CategoryVisible}, ConverterParameter=general}">
                    <TextBlock Style="{StaticResource SectionHeaderStyle}" Text="{loc:Loc SettingsCat_General}" />

                    <Border Style="{StaticResource CardStyle}" Tag="general.restoreSession">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0" Margin="0,0,16,0">
                                <TextBlock Text="{loc:Loc Settings_RestoreSession}" />
                                <TextBlock Style="{StaticResource CaptionStyle}" Text="{loc:Loc Settings_RestoreSessionHint}" />
                            </StackPanel>
                            <ui:ToggleSwitch Grid.Column="1" VerticalAlignment="Center" IsChecked="{Binding RestoreSession, Mode=TwoWay}" />
                        </Grid>
                    </Border>

                    <Border Style="{StaticResource CardStyle}" Tag="general.startupFolder" IsEnabled="{Binding RestoreSession, Converter={StaticResource InvertBool}}">
                        <StackPanel>
                            <TextBlock Text="{loc:Loc Settings_StartupFolder}" />
                            <TextBlock Style="{StaticResource CaptionStyle}" Text="{loc:Loc Settings_StartupFolderHint}" />
                            <ui:TextBox Margin="0,10,0,0" Text="{Binding StartupFolder, Mode=TwoWay, UpdateSourceTrigger=LostFocus}" />
                        </StackPanel>
                    </Border>

                    <Border Style="{StaticResource CardStyle}" Tag="general.singleInstance">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0" Margin="0,0,16,0">
                                <TextBlock Text="{loc:Loc Settings_SingleInstance}" />
                                <TextBlock Style="{StaticResource CaptionStyle}" Text="{loc:Loc Settings_SingleInstanceHint}" />
                            </StackPanel>
                            <ui:ToggleSwitch Grid.Column="1" VerticalAlignment="Center" IsChecked="{Binding SingleInstance, Mode=TwoWay}" />
                        </Grid>
                    </Border>
                </StackPanel>

                <!--  ===== Integrációk: terminál =====  -->
                <StackPanel Visibility="{Binding SelectedCategory.Id, Converter={StaticResource CategoryVisible}, ConverterParameter=integrations}">
                    <Border Style="{StaticResource CardStyle}" Tag="integrations.terminal">
                        <StackPanel>
                            <TextBlock Text="{loc:Loc Settings_Terminal}" />
                            <TextBlock Style="{StaticResource CaptionStyle}" Text="{loc:Loc Settings_TerminalHint}" />
                            <ui:TextBox Margin="0,10,0,0" Text="{Binding ExternalTerminalPath, Mode=TwoWay, UpdateSourceTrigger=LostFocus}" />
                        </StackPanel>
                    </Border>
                </StackPanel>
                <!--  ===== Rendszerintegráció =====  -->
                <TextBlock Style="{StaticResource SectionHeaderStyle}" Text="{loc:Loc Settings_ShellIntegration}" />
                <TextBlock
                    Margin="2,0,0,10"
                    Style="{StaticResource CaptionStyle}"
                    Text="{loc:Loc Settings_ShellIntegrationHint}" />

                <Border Style="{StaticResource CardStyle}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" Margin="0,0,16,0">
                            <TextBlock Text="{loc:Loc ShellIntegration_FolderOpen}" />
                            <TextBlock Style="{StaticResource CaptionStyle}" Text="{loc:Loc ShellIntegration_FolderOpenHint}" />
                        </StackPanel>
                        <ui:ToggleSwitch
                            x:Name="FolderOpenToggle"
                            Grid.Column="1"
                            VerticalAlignment="Center"
                            IsChecked="{Binding FolderOpenRedirectEnabled, Mode=OneWay}"
                            PreviewMouseLeftButtonDown="OnFolderOpenRedirectPreviewClick" />
                    </Grid>
                </Border>

                <Border Style="{StaticResource CardStyle}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" Margin="0,0,16,0">
                            <TextBlock Text="{loc:Loc ShellIntegration_WinE}" />
                            <TextBlock Style="{StaticResource CaptionStyle}" Text="{loc:Loc ShellIntegration_WinEHint}" />
                        </StackPanel>
                        <ui:ToggleSwitch
                            Grid.Column="1"
                            VerticalAlignment="Center"
                            IsChecked="{Binding WinERedirectEnabled, Mode=OneWay}"
                            PreviewMouseLeftButtonDown="OnWinERedirectPreviewClick" />
                    </Grid>
                </Border>

                <Border Style="{StaticResource CardStyle}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" Margin="0,0,16,0">
                            <TextBlock Text="{loc:Loc ShellIntegration_ContextMenu}" />
                            <TextBlock Style="{StaticResource CaptionStyle}" Text="{loc:Loc ShellIntegration_ContextMenuHint}" />
                        </StackPanel>
                        <ui:ToggleSwitch
                            Grid.Column="1"
                            VerticalAlignment="Center"
                            IsChecked="{Binding ContextMenuEntryEnabled, Mode=OneWay}"
                            PreviewMouseLeftButtonDown="OnContextMenuEntryPreviewClick" />
                    </Grid>
                </Border>

                <Border Style="{StaticResource CardStyle}">
                    <StackPanel>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0" Margin="0,0,16,0">
                                <TextBlock Text="{loc:Loc ShellIntegration_ResetAll}" />
                                <TextBlock Style="{StaticResource CaptionStyle}" Text="{loc:Loc ShellIntegration_ResetAllHint}" />
                            </StackPanel>
                            <ui:Button
                                Grid.Column="1"
                                VerticalAlignment="Center"
                                Command="{Binding ResetShellIntegrationCommand}"
                                Content="{loc:Loc Cmd_Reset}" />
                        </Grid>
                        <TextBlock
                            Margin="0,10,0,0"
                            Foreground="{DynamicResource TokenDanger}"
                            Text="{Binding ShellIntegrationError}"
                            TextWrapping="Wrap"
                            Visibility="{Binding ShellIntegrationError, Converter={StaticResource NullToCollapsed}}" />
                    </StackPanel>
                </Border>

                </StackPanel>

                <StackPanel Visibility="{Binding SelectedCategory.Id, Converter={StaticResource CategoryVisible}, ConverterParameter=keyboard}">
                <!--  ===== Billentyűkiosztás =====  -->
                <TextBlock Style="{StaticResource SectionHeaderStyle}" Text="{loc:Loc Settings_Keymap}" />

                <Border Style="{StaticResource CardStyle}">
                    <StackPanel>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0" Margin="0,0,16,0">
                                <TextBlock Text="{loc:Loc Settings_Keymap}" />
                                <TextBlock Style="{StaticResource CaptionStyle}" Text="{loc:Loc Settings_KeymapHint}" TextWrapping="Wrap" />
                            </StackPanel>
                            <ComboBox
                                Grid.Column="1"
                                Width="240"
                                VerticalAlignment="Center"
                                ItemsSource="{Binding Keymaps}"
                                SelectedItem="{Binding SelectedKeymap, Mode=TwoWay}">
                                <ComboBox.ItemTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding Converter={StaticResource KeymapName}}" />
                                    </DataTemplate>
                                </ComboBox.ItemTemplate>
                            </ComboBox>
                        </Grid>

                        <ui:Button
                            Margin="0,12,0,0"
                            HorizontalAlignment="Left"
                            Command="{Binding ToggleKeymapTableCommand}"
                            Content="{loc:Loc Keymap_ViewTable}" />

                        <!--  A preset teljes billentyűlistája (spec F1)  -->
                        <Border
                            Margin="0,12,0,0"
                            Background="{DynamicResource TokenBgApp}"
                            BorderBrush="{DynamicResource TokenBorder}"
                            BorderThickness="1"
                            CornerRadius="6"
                            Visibility="{Binding IsKeymapTableVisible, Converter={StaticResource BoolToVisibility}}">
                            <ScrollViewer MaxHeight="320" VerticalScrollBarVisibility="Auto">
                                <ItemsControl ItemsSource="{Binding KeymapRows}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Grid Margin="12,7">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="150" />
                                                    <ColumnDefinition Width="120" />
                                                    <ColumnDefinition Width="*" />
                                                </Grid.ColumnDefinitions>
                                                <TextBlock
                                                    Grid.Column="0"
                                                    FontWeight="SemiBold"
                                                    Text="{Binding Action}"
                                                    TextTrimming="CharacterEllipsis" />
                                                <TextBlock
                                                    Grid.Column="1"
                                                    FontFamily="Consolas"
                                                    Foreground="{DynamicResource TokenAccent}"
                                                    Text="{Binding Gesture}" />
                                                <TextBlock
                                                    Grid.Column="2"
                                                    Foreground="{DynamicResource TokenTextSecondary}"
                                                    Text="{Binding Description}"
                                                    TextWrapping="Wrap" />
                                            </Grid>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </ScrollViewer>
                        </Border>
                    </StackPanel>
                </Border>

                <Border Style="{StaticResource CardStyle}">
                    <StackPanel>
                        <TextBlock Text="{loc:Loc Settings_ExternalEditor}" />
                        <TextBlock Style="{StaticResource CaptionStyle}" Text="{loc:Loc Settings_ExternalEditorHint}" />
                        <Grid Margin="0,10,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <ui:TextBox
                                Grid.Column="0"
                                VerticalAlignment="Center"
                                Text="{Binding ExternalEditorPath, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                            <ui:Button
                                Grid.Column="1"
                                Margin="8,0,0,0"
                                Command="{Binding BrowseExternalEditorCommand}"
                                Content="{loc:Loc Cmd_Browse}" />
                        </Grid>
                    </StackPanel>
                </Border>

                </StackPanel>

                <StackPanel Visibility="{Binding SelectedCategory.Id, Converter={StaticResource CategoryVisible}, ConverterParameter=general}">
                <!--  ===== Nyelv =====  -->
                <TextBlock Style="{StaticResource SectionHeaderStyle}" Text="{loc:Loc Settings_Language}" />

                <Border Style="{StaticResource CardStyle}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" Margin="0,0,16,0">
                            <TextBlock Text="{loc:Loc Settings_Language}" />
                            <TextBlock Style="{StaticResource CaptionStyle}" Text="{loc:Loc Settings_LanguageHint}" />
                        </StackPanel>
                        <ComboBox
                            Grid.Column="1"
                            Width="200"
                            VerticalAlignment="Center"
                            DisplayMemberPath="DisplayName"
                            ItemsSource="{Binding Languages}"
                            SelectedItem="{Binding SelectedLanguage, Mode=TwoWay}" />
                    </Grid>
                </Border>

                </StackPanel>

                <StackPanel Visibility="{Binding SelectedCategory.Id, Converter={StaticResource CategoryVisible}, ConverterParameter=advanced}">
                <!--  ===== Gyorsgombok =====  -->
                <TextBlock Style="{StaticResource SectionHeaderStyle}" Text="{loc:Loc Settings_QuickActions}" />
                <TextBlock
                    Margin="2,0,0,10"
                    Style="{StaticResource CaptionStyle}"
                    Text="{loc:Loc Settings_QuickActionsHint}" />

                <ItemsControl ItemsSource="{Binding QuickActions}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate DataType="{x:Type vm:QuickActionEditorViewModel}">
                            <Border Style="{StaticResource CardStyle}" Padding="16">
                                <StackPanel>

                                    <TextBlock
                                        Margin="0,0,0,12"
                                        FontWeight="SemiBold"
                                        Text="{Binding Header, Converter={StaticResource LocalizeKey}}" />

                                    <!--
                                        Típus. A GroupName a szerkesztő fejlécéből jön, hogy a két
                                        gyorsgomb rádiói biztosan külön csoportba essenek — enélkül
                                        a második szerkesztő választása kikapcsolná az elsőét.
                                    -->
                                    <StackPanel Margin="0,0,0,12" Orientation="Horizontal">
                                        <RadioButton
                                            Margin="0,0,18,0"
                                            Content="{loc:Loc Type_Folder}"
                                            GroupName="{Binding Header}"
                                            IsChecked="{Binding Kind, Mode=TwoWay, Converter={StaticResource EnumToBool}, ConverterParameter=Folder}" />
                                        <RadioButton
                                            Content="{loc:Loc Type_File}"
                                            GroupName="{Binding Header}"
                                            IsChecked="{Binding Kind, Mode=TwoWay, Converter={StaticResource EnumToBool}, ConverterParameter=File}" />
                                    </StackPanel>

                                    <!--  Kiterjesztés — csak fájlnál  -->
                                    <StackPanel Margin="0,0,0,12" Visibility="{Binding IsFolder, Converter={StaticResource BoolToVisibility}, ConverterParameter=invert}">
                                        <TextBlock Margin="0,0,0,4" Text="{loc:Loc Settings_Extension}" />
                                        <ui:TextBox Width="120" HorizontalAlignment="Left" Text="{Binding Extension, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                                    </StackPanel>

                                    <!--  Névsablon + élő előnézet  -->
                                    <TextBlock Margin="0,0,0,4" Text="{loc:Loc Settings_NameTemplate}" />
                                    <ui:TextBox Text="{Binding NameTemplate, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                                    <TextBlock Style="{StaticResource CaptionStyle}" Text="{loc:Loc Settings_TemplateHint}" />

                                    <Border
                                        Margin="0,8,0,12"
                                        Padding="10,6"
                                        Background="{DynamicResource SubtleFillColorSecondaryBrush}"
                                        CornerRadius="6">
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock
                                                Margin="0,0,8,0"
                                                Foreground="{DynamicResource TextFillColorTertiaryBrush}"
                                                Text="{loc:Loc Settings_Preview}" />
                                            <TextBlock FontWeight="SemiBold" Text="{Binding Preview}" />
                                        </StackPanel>
                                    </Border>

                                    <!--  Cél  -->
                                    <CheckBox
                                        Margin="0,0,0,8"
                                        Content="{loc:Loc Settings_UseFixedPath}"
                                        IsChecked="{Binding UseFixedPath, Mode=TwoWay}" />

                                    <Grid Visibility="{Binding UseFixedPath, Converter={StaticResource BoolToVisibility}}">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>
                                        <ui:TextBox
                                            Grid.Column="0"
                                            Margin="0,0,8,0"
                                            Text="{Binding FixedPath, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                                        <ui:Button
                                            Grid.Column="1"
                                            Command="{Binding BrowseFolderCommand}"
                                            Content="{loc:Loc Settings_Browse}" />
                                    </Grid>

                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <TextBlock
                    Margin="2,10,0,0"
                    Style="{StaticResource CaptionStyle}"
                    Text="{loc:Loc Settings_AutoSaved}" />

                </StackPanel>

                <StackPanel Visibility="{Binding SelectedCategory.Id, Converter={StaticResource CategoryVisible}, ConverterParameter=tags}">
                <!--  ===== Címkék =====  -->
                <TextBlock Style="{StaticResource SectionHeaderStyle}" Text="{loc:Loc Nav_Tags}" />
                <TextBlock
                    Margin="2,0,0,10"
                    Style="{StaticResource CaptionStyle}"
                    Text="{loc:Loc Settings_TagsHint}" />

                <Border Style="{StaticResource CardStyle}">
                    <StackPanel>
                        <ItemsControl ItemsSource="{Binding Tags}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate DataType="{x:Type vm:TagEditorViewModel}">
                                    <Grid Margin="0,0,0,8">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto" />
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>

                                        <!--
                                            Színminta ÉS színválasztó gomb egyben — lásd
                                            Controls/TagSwatch.cs. A minta a címke saját
                                            színével van kitöltve (egyedi hexszel is), és
                                            mindig kap szegélyt, hogy világos témában egy
                                            fehér címke se olvadjon bele a háttérbe.
                                        -->
                                        <ui:Button
                                            x:Name="ColorButton"
                                            Grid.Column="0"
                                            Margin="0,0,10,0"
                                            Padding="5"
                                            VerticalAlignment="Center"
                                            Appearance="Transparent"
                                            Command="{Binding TogglePickerCommand}"
                                            ToolTip="{loc:Loc Tags_PickColor}">
                                            <controls:TagSwatch ColorHex="{Binding ColorHex}" TagColor="{Binding Color}" />
                                        </ui:Button>

                                        <Popup
                                            Grid.Column="0"
                                            AllowsTransparency="True"
                                            IsOpen="{Binding IsPickerOpen, Mode=TwoWay}"
                                            Placement="Bottom"
                                            PlacementTarget="{Binding ElementName=ColorButton}"
                                            StaysOpen="False">
                                            <Border
                                                Padding="12"
                                                Background="{DynamicResource TokenBgElevated}"
                                                BorderBrush="{DynamicResource TokenBorder}"
                                                BorderThickness="1"
                                                CornerRadius="8">
                                                <StackPanel Width="216">
                                                    <ItemsControl ItemsSource="{Binding Presets}">
                                                        <ItemsControl.ItemsPanel>
                                                            <ItemsPanelTemplate>
                                                                <UniformGrid Columns="6" />
                                                            </ItemsPanelTemplate>
                                                        </ItemsControl.ItemsPanel>
                                                        <ItemsControl.ItemTemplate>
                                                            <DataTemplate DataType="{x:Type metadata:TagColor}">
                                                                <ui:Button
                                                                    Margin="2"
                                                                    Padding="4"
                                                                    Appearance="Transparent"
                                                                    Command="{Binding DataContext.SelectPresetCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                                                    CommandParameter="{Binding}">
                                                                    <controls:TagSwatch
                                                                        Width="20"
                                                                        Height="20"
                                                                        TagColor="{Binding}" />
                                                                </ui:Button>
                                                            </DataTemplate>
                                                        </ItemsControl.ItemTemplate>
                                                    </ItemsControl>

                                                    <TextBlock
                                                        Margin="2,10,0,4"
                                                        FontSize="12"
                                                        Foreground="{DynamicResource TokenTextSecondary}"
                                                        Text="{loc:Loc Tags_CustomColor}" />
                                                    <Grid>
                                                        <Grid.ColumnDefinitions>
                                                            <ColumnDefinition Width="Auto" />
                                                            <ColumnDefinition Width="*" />
                                                            <ColumnDefinition Width="Auto" />
                                                        </Grid.ColumnDefinitions>
                                                        <!--  Élő előnézet: a beírt hex azonnal látszik, még alkalmazás előtt.  -->
                                                        <controls:TagSwatch
                                                            Grid.Column="0"
                                                            Width="24"
                                                            Height="24"
                                                            Margin="0,0,8,0"
                                                            VerticalAlignment="Center"
                                                            ColorHex="{Binding CustomHexInput}"
                                                            TagColor="{Binding Color}" />
                                                        <ui:TextBox
                                                            Grid.Column="1"
                                                            VerticalAlignment="Center"
                                                            PlaceholderText="#RRGGBB"
                                                            Text="{Binding CustomHexInput, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                                                        <ui:Button
                                                            Grid.Column="2"
                                                            Margin="8,0,0,0"
                                                            Command="{Binding ApplyCustomHexCommand}"
                                                            Content="{loc:Loc Cmd_Apply}" />
                                                    </Grid>
                                                </StackPanel>
                                            </Border>
                                        </Popup>

                                        <ui:TextBox
                                            Grid.Column="1"
                                            Margin="0,0,8,0"
                                            Text="{Binding Name, Mode=TwoWay, UpdateSourceTrigger=LostFocus}" />
                                        <ui:Button
                                            Grid.Column="2"
                                            Padding="6"
                                            Appearance="Transparent"
                                            Command="{Binding DataContext.DeleteTagCommand, RelativeSource={RelativeSource AncestorType=ui:FluentWindow}}"
                                            CommandParameter="{Binding}"
                                            ToolTip="{loc:Loc Cmd_Delete}">
                                            <ui:SymbolIcon FontSize="14" Symbol="Delete24" />
                                        </ui:Button>
                                    </Grid>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>

                        <!--  Új címke: szín + név + hozzáadás  -->
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <ComboBox
                                Grid.Column="0"
                                Width="62"
                                Margin="0,0,8,0"
                                ItemsSource="{Binding TagColors}"
                                SelectedItem="{Binding NewTagColor, Mode=TwoWay}">
                                <ComboBox.ItemTemplate>
                                    <DataTemplate DataType="{x:Type metadata:TagColor}">
                                        <controls:TagSwatch TagColor="{Binding}" />
                                    </DataTemplate>
                                </ComboBox.ItemTemplate>
                            </ComboBox>
                            <ui:TextBox
                                Grid.Column="1"
                                Margin="0,0,8,0"
                                PlaceholderText="{loc:Loc Settings_NewTagPlaceholder}"
                                Text="{Binding NewTagName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                            <ui:Button
                                Grid.Column="2"
                                Command="{Binding AddTagCommand}"
                                Content="{loc:Loc Cmd_Add}" />
                        </Grid>
                    </StackPanel>
                </Border>

                </StackPanel>

                <StackPanel Visibility="{Binding SelectedCategory.Id, Converter={StaticResource CategoryVisible}, ConverterParameter=about}">
                <!--  ===== Frissítések =====  -->
                <TextBlock Style="{StaticResource SectionHeaderStyle}" Text="{loc:Loc Settings_Updates}" />

                <Border Style="{StaticResource CardStyle}" DataContext="{Binding Updates}">
                    <StackPanel>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0" Margin="0,0,16,0">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="{loc:Loc Settings_CurrentVersion}" />
                                    <!--
                                        Nincs szándékos vizuális jelzés (kurzor,
                                        aláhúzás) — lásd a Hibabejelentés fejlécének
                                        10-kattintásos gesztusát fentebb, ugyanaz a
                                        minta: SettingsViewModel.RegisterVersionClick,
                                        7 kattintásra rejtett üzenet bukkan fel.
                                    -->
                                    <TextBlock
                                        Margin="6,0,0,0"
                                        FontWeight="SemiBold"
                                        MouseLeftButtonDown="OnVersionClick"
                                        Text="{Binding CurrentVersion, StringFormat=v{0}}" />
                                </StackPanel>
                                <TextBlock Style="{StaticResource CaptionStyle}" Text="{loc:Loc Settings_UpdatesHint}" />
                            </StackPanel>
                            <Grid Grid.Column="1" VerticalAlignment="Center">
                                <ui:ProgressRing
                                    Width="18"
                                    Height="18"
                                    IsIndeterminate="True"
                                    Visibility="{Binding IsBusy, Converter={StaticResource BoolToVisibility}}" />
                                <ui:Button
                                    Command="{Binding CheckNowCommand}"
                                    Content="{loc:Loc Update_CheckNow}"
                                    Visibility="{Binding IsBusy, Converter={StaticResource BoolToVisibility}, ConverterParameter=invert}" />
                            </Grid>
                        </Grid>
                        <TextBlock
                            Margin="0,10,0,0"
                            Style="{StaticResource CaptionStyle}"
                            Text="{Binding StatusMessage}"
                            Visibility="{Binding StatusMessage, Converter={StaticResource NullToCollapsed}}" />
                    </StackPanel>
                </Border>

                </StackPanel>

                <StackPanel Visibility="{Binding SelectedCategory.Id, Converter={StaticResource CategoryVisible}, ConverterParameter=integrations}">
                <!--  ===== Hibabejelentés =====  -->
                <!--
                    A fejlécre kattintás nem véletlen: 10 kattintásra nyílik
                    fel a lenti fejlesztői (Discord-integrációs) panel — lásd
                    OnBugReportHeaderClick és BugReportViewModel.RegisterSecretClick.
                    Szándékosan nincs vizuális jelzés (kurzor, aláhúzás), hogy a
                    hétköznapi felhasználó ne is sejtse.
                -->
                <TextBlock
                    MouseLeftButtonDown="OnBugReportHeaderClick"
                    Style="{StaticResource SectionHeaderStyle}"
                    Text="{loc:Loc Settings_BugReport}" />
                <TextBlock
                    Margin="2,0,0,10"
                    Style="{StaticResource CaptionStyle}"
                    Text="{loc:Loc Settings_BugReportHint}" />

                <!--
                    Alapállapot: csak egy egyszerű, publikus e-mail-cím — a
                    végfelhasználót nem kell terhelni a Discord-integráció
                    részleteivel (webhook/bot-állapot, csatolások stb.).
                -->
                <Border
                    Style="{StaticResource CardStyle}"
                    Padding="16"
                    DataContext="{Binding BugReport}"
                    Visibility="{Binding IsAdminPanelUnlocked, Converter={StaticResource BoolToVisibility}, ConverterParameter=invert}">
                    <TextBlock TextWrapping="Wrap">
                        <Run Text="{loc:Loc Settings_BugReportSimpleMessage}" />
                        <LineBreak />
                        <Hyperlink NavigateUri="mailto:pilaster-explorer@proton.me" RequestNavigate="OnSupportEmailNavigate">
                            <Run Text="pilaster-explorer@proton.me" />
                        </Hyperlink>
                    </TextBlock>
                </Border>

                <!--
                    A teljes fejlesztői panel — csak feloldás után látszik,
                    lásd fent.
                -->
                <Border
                    Style="{StaticResource CardStyle}"
                    Padding="16"
                    DataContext="{Binding BugReport}"
                    Visibility="{Binding IsAdminPanelUnlocked, Converter={StaticResource BoolToVisibility}}">
                    <StackPanel>

                        <TextBlock Margin="0,0,0,4" Text="{loc:Loc BugReport_DescriptionLabel}" />
                        <ui:TextBox
                            MinHeight="90"
                            MaxHeight="160"
                            AcceptsReturn="True"
                            IsEnabled="{Binding IsConfigured}"
                            PlaceholderText="{loc:Loc BugReport_DescriptionPlaceholder}"
                            Text="{Binding Description, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                            TextWrapping="Wrap"
                            VerticalScrollBarVisibility="Auto" />

                        <StackPanel Margin="0,10,0,0" Orientation="Horizontal">
                            <CheckBox
                                Margin="0,0,20,0"
                                Content="{loc:Loc BugReport_AttachScreenshot}"
                                IsChecked="{Binding AttachScreenshot, Mode=TwoWay}"
                                IsEnabled="{Binding IsConfigured}" />
                            <CheckBox
                                Content="{loc:Loc BugReport_AttachLog}"
                                IsChecked="{Binding AttachLog, Mode=TwoWay}"
                                IsEnabled="{Binding IsConfigured}" />
                        </StackPanel>

                        <!--
                            Ötlet vs. hiba. A Discord üzenet ez alapján kap
                            [BUG]/[ÖTLET] címkét és eltérő színt — lásd
                            DiscordPayloadBuilder.
                        -->
                        <CheckBox
                            Margin="0,10,0,0"
                            Content="{loc:Loc BugReport_IsFeatureIdea}"
                            IsChecked="{Binding IsFeatureIdea, Mode=TwoWay}"
                            IsEnabled="{Binding IsConfigured}" />

                        <!--  Visszajelzés-sáv: siker zöld pipával, hiba piros felkiáltójellel  -->
                        <Border
                            Margin="0,12,0,0"
                            Padding="10,7"
                            Background="{DynamicResource SubtleFillColorSecondaryBrush}"
                            CornerRadius="6"
                            Visibility="{Binding StatusVisible, Converter={StaticResource BoolToVisibility}}">
                            <StackPanel Orientation="Horizontal">
                                <ui:SymbolIcon
                                    Margin="0,0,8,0"
                                    VerticalAlignment="Top"
                                    Foreground="{DynamicResource TokenSuccess}"
                                    Symbol="CheckmarkCircle24"
                                    Visibility="{Binding StatusIsError, Converter={StaticResource BoolToVisibility}, ConverterParameter=invert}" />
                                <ui:SymbolIcon
                                    Margin="0,0,8,0"
                                    VerticalAlignment="Top"
                                    Foreground="{DynamicResource TokenDanger}"
                                    Symbol="ErrorCircle24"
                                    Visibility="{Binding StatusIsError, Converter={StaticResource BoolToVisibility}}" />
                                <TextBlock
                                    VerticalAlignment="Center"
                                    Text="{Binding StatusMessage}"
                                    TextWrapping="Wrap" />
                            </StackPanel>
                        </Border>

                        <Grid Margin="0,12,0,0">
                            <ui:ProgressRing
                                Width="18"
                                Height="18"
                                HorizontalAlignment="Right"
                                IsIndeterminate="True"
                                Visibility="{Binding IsSending, Converter={StaticResource BoolToVisibility}}" />
                            <ui:Button
                                HorizontalAlignment="Right"
                                Appearance="Primary"
                                Command="{Binding SendCommand}"
                                Content="{loc:Loc BugReport_Send}"
                                Visibility="{Binding IsSending, Converter={StaticResource BoolToVisibility}, ConverterParameter=invert}">
                                <ui:Button.Icon>
                                    <ui:SymbolIcon Symbol="Send24" />
                                </ui:Button.Icon>
                            </ui:Button>
                        </Grid>

                    </StackPanel>
                </Border>

                </StackPanel>

            </StackPanel>
        </ScrollViewer>

            <!--  Alsó sáv: kategória visszaállítása + teljes export/import (spec F6)  -->
            <Border
                Grid.Row="2"
                Grid.Column="0"
                Grid.ColumnSpan="2"
                Margin="18,10,18,14">
                <Grid>
                    <StackPanel HorizontalAlignment="Left" Orientation="Horizontal">
                        <ui:Button
                            Margin="0,0,6,0"
                            Command="{Binding ResetCategoryCommand}"
                            Content="{loc:Loc Settings_ResetCategory}" />
                        <ui:Button
                            Margin="0,0,6,0"
                            Command="{Binding ImportSettingsCommand}"
                            Content="{loc:Loc QuickAccess_Import}" />
                        <ui:Button
                            Command="{Binding ExportSettingsCommand}"
                            Content="{loc:Loc QuickAccess_Export}" />
                    </StackPanel>
                    <TextBlock
                        HorizontalAlignment="Right"
                        VerticalAlignment="Center"
                        FontSize="12"
                        Foreground="{DynamicResource TokenTextSecondary}"
                        Text="{Binding StatusMessage}"
                        Visibility="{Binding StatusMessage, Converter={StaticResource NullToCollapsed}}" />
                </Grid>
            </Border>
        </Grid>
        </Border>

        <!--
            Rejtett üzenet-toast: a verziószámra 7-szer kattintva (lásd
            OnVersionClick / SettingsViewModel.RegisterVersionClick) bukkan
            fel, magától eltűnik — semmilyen tartós állapotot nem módosít.
        -->
        <Border
            Margin="0,0,0,24"
            Padding="16,12"
            HorizontalAlignment="Center"
            VerticalAlignment="Bottom"
            Background="{DynamicResource CardBackgroundFillColorDefaultBrush}"
            BorderBrush="{DynamicResource SystemAccentBrush}"
            BorderThickness="1.5"
            CornerRadius="10"
            IsHitTestVisible="False"
            Opacity="0">
            <Border.Effect>
                <DropShadowEffect BlurRadius="18" Opacity="0.35" ShadowDepth="0" />
            </Border.Effect>
            <Border.Style>
                <Style TargetType="Border">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding ShowEasterEgg}" Value="True">
                            <DataTrigger.EnterActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation
                                            Storyboard.TargetProperty="Opacity"
                                            To="1"
                                            Duration="0:0:0.2" />
                                    </Storyboard>
                                </BeginStoryboard>
                            </DataTrigger.EnterActions>
                            <DataTrigger.ExitActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation
                                            Storyboard.TargetProperty="Opacity"
                                            To="0"
                                            Duration="0:0:0.3" />
                                    </Storyboard>
                                </BeginStoryboard>
                            </DataTrigger.ExitActions>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Border.Style>
            <StackPanel Orientation="Horizontal">
                <ui:SymbolIcon
                    Margin="0,0,10,0"
                    VerticalAlignment="Center"
                    FontSize="18"
                    Foreground="{DynamicResource SystemAccentBrush}"
                    Symbol="Sparkle24" />
                <TextBlock
                    MaxWidth="320"
                    VerticalAlignment="Center"
                    Text="{Binding EasterEggMessage}"
                    TextWrapping="Wrap" />
            </StackPanel>
        </Border>
        </Grid>
    </Grid>
</ui:FluentWindow>
