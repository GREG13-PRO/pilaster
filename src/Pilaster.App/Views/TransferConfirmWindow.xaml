<ui:FluentWindow
    x:Class="Pilaster.App.Views.TransferConfirmWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:loc="clr-namespace:Pilaster.App.Localization"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    Width="480"
    Height="220"
    MinWidth="360"
    mc:Ignorable="d"
    ExtendsContentIntoTitleBar="True"
    ResizeMode="NoResize"
    WindowBackdropType="Mica"
    WindowCornerPreference="Round"
    WindowStartupLocation="CenterOwner">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <ui:TitleBar
            x:Name="TitleBarHost"
            Grid.Row="0"
            ShowMaximize="False"
            ShowMinimize="False" />

        <StackPanel Grid.Row="1" Margin="20,4,20,20">
            <TextBlock
                x:Name="SummaryText"
                Margin="0,0,0,4"
                FontWeight="SemiBold" />
            <TextBlock
                x:Name="HintText"
                Margin="0,0,0,10"
                FontSize="12"
                Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                TextWrapping="Wrap" />
            <ui:TextBox
                x:Name="TargetBox"
                KeyDown="OnTargetBoxKeyDown" />

            <StackPanel
                Margin="0,20,0,0"
                HorizontalAlignment="Right"
                Orientation="Horizontal">
                <ui:Button
                    Margin="0,0,8,0"
                    Click="OnCancelClick"
                    Content="{loc:Loc Cmd_Cancel}" />
                <ui:Button
                    Appearance="Primary"
                    Click="OnOkClick"
                    Content="{loc:Loc Cmd_Ok}" />
            </StackPanel>
        </StackPanel>
    </Grid>
</ui:FluentWindow>
